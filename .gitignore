# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
pnpm-workspace.yaml
lerna-debug.log*

node_modules
# dist 随库发布：服务端一键升级从 GitHub 公开仓 clone（含子模块），前端产物必须
# 在库里才能免构建部署。publish_github.sh 发布前会用 vite 重构建（绕开 tsc 存量报错）。
# dist-ssr 仍是纯本地产物，不随库。
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
