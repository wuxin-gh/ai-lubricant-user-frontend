# MonkeyCode AI 前端使用文档

本文档基于当前 `frontend` 源码整理，描述的是现在前端已经实现的页面、入口和交互。

如果文档与页面行为不一致，优先以以下源码为准：

- 路由入口：`frontend/src/App.tsx`
- 页面组件：`frontend/src/pages/**`
- 关键交互组件：`frontend/src/components/**`

---

## 一、产品与页面入口

### 1. 当前前端覆盖的能力

当前前端已经覆盖以下能力：

- 官网首页与公开介绍页
- 普通用户登录与控制台
- 智能任务创建、执行、追问、上下文管理、文件查看和在线预览
- 项目协作、需求管理和自动 Review
- Git 审查机器人
- 独立开发环境、终端、文件管理、端口管理和远程协助
- 开发者广场内容浏览与发布
- 企业管理后台

### 2. 主要路由

- 首页：`/`
- 登录页：`/login`
- 找回密码：`/findpassword`
- 重置密码：`/resetpassword?token=...`
- 开发者广场：`/playground`
- 发布广场内容：`/playground/create`
- 隐私政策：`/privacy-policy`
- 用户协议：`/user-agreement`
- 产品介绍 PPT：`/ppt/` 或 `/ppt/index.html`
- 公开任务跳转入口：`/tasks/public?id=...`
- 普通用户控制台：`/console`
- 新任务页：`/console/tasks`
- 任务详情页：`/console/task/:taskId`
- 项目页：`/console/project/:projectId`
- Git 机器人：`/console/gitbot`
- IDE 辅助工具：`/console/ide`
- 独立终端页：`/console/terminal?envid=...`
- 独立文件管理页：`/console/files?envid=...&path=...`
- 远程协助页：`/sharedterminal?id=...`
- 企业管理后台：`/manager`

### 3. 首页导航

首页顶部导航当前包含：

- `介绍`
- `广场`
- `文档` / `使用文档`
- `开源` / GitHub 入口
- 已登录时显示 `控制台` / `进入控制台`
- 未登录时显示 `注册`、`立即开始`

说明：

- `文档` / `使用文档` 当前跳转到外部文档站：`https://monkeycode.docs.baizhi.cloud/`
- `开源` / GitHub 当前跳转：`https://github.com/chaitin/MonkeyCode`
- 访问首页时如果 URL 带 `?ic=...`，前端会把邀请码写入 `localStorage`，注册登录入口会携带该邀请 ID
- 移动端导航收纳在抽屉菜单中

### 4. 首页内容

首页当前使用 `TerminalNativePage`，主要区块包括：

- Hero：`MonkeyCode` 在线 AI 开发平台介绍
- 功能与特色：免费即用、云端开发环境、主流模型、移动端支持、开源、私有化离线部署
- 使用场景：小游戏、需求开发、安全审查、毕业论文、数据分析、技术调研等
- 和其他 Coding 工具的能力对比
- 用户真实评价
- 套餐与费用
- 免费赚积分与充值积分
- 开源版与团队版入口
- FAQ
- 底部开始使用和 GitHub 入口

### 5. 套餐与积分入口

首页展示的套餐包括：

- `基础版`：免费，1 个并发任务，云开发环境 1C / 4GB，注册赠送 5000 积分
- `专业版`：10,000 积分 / 月，3 个并发任务，云开发环境 2C / 8GB，每日赠送 2,000 积分
- `旗舰版`：100,000 积分 / 月，3 个并发任务，云开发环境 2C / 8GB，每日赠送 30,000 积分

首页还展示：

- 注册即送 5000 积分
- 每邀请 1 位新用户奖励 5000 积分
- 每日签到可持续获得积分
- 分享使用故事可获得活动积分
- 充值档位：¥50 / 1 万积分、¥200 / 5 万积分、¥1000 / 30 万积分

### 6. 客户端下载

代码中仍保留了 `downloads` 组件，支持展示：

- Windows
- macOS
- Android
- iOS

下载统一跳转：

- `https://github.com/chaitin/MonkeyCode/releases`

但当前首页实际渲染的是新版 `TerminalNativePage`，不再直接展示旧版客户端下载区块。

### 7. 页脚与法律页面

公开页面页脚包含：

- 产品文档
- 技术论坛
- 开源仓库
- 模型广场
- 长亭科技
- 长亭百智云
- 隐私政策
- 用户协议
- ICP 备案链接
- 微信群、飞书群、钉钉群二维码

法律页面包括：

- `/privacy-policy`：隐私政策，最后更新时间为 `2026-03-24`
- `/user-agreement`：用户协议，最后更新时间为 `2026-03-24`

法律页面顶部导航会切换为 `隐私政策` 和 `用户协议` 两个入口。

### 8. 产品介绍 PPT

`frontend/public/ppt/index.html` 是一个基于 Reveal.js 的静态产品介绍 PPT。

部署后可通过以下路径访问：

- `/ppt/`
- `/ppt/index.html`

内容主题包括：

- MonkeyCode AI 产品定位
- 需求、设计、编码、审查闭环
- 智能任务、项目协作和自动 Review
- 多模型、自定义镜像、自有宿主机和消息通知
- 企业管理后台、开发者广场和开源社区

说明：

- 该 PPT 使用 CDN 加载 Reveal.js、主题样式和字体
- 这是静态介绍页，不经过 React 路由
- 当前文件不是 `.pptx`，而是 HTML 演示文稿

---

## 二、登录与账号

### 1. 登录页

登录页路径：

- `/login`

页面包含两个标签：

- `普通用户`
- `团队管理员`

登录页底部要求勾选并同意：

- `用户协议`

如果未勾选协议，普通用户 OAuth 登录、快速注册、账号密码登录和团队管理员登录都会被阻止。

### 2. 普通用户登录

普通用户标签默认先展示登录方式选择：

- `百智云登录 - 推荐`
- `账号密码登录`
- `快速注册`

说明：

- `百智云登录 - 推荐` 和 `快速注册` 都跳转到后端登录入口 `/api/v1/users/login?redirect=&inviter_id=...`
- 如果首页或注册入口保存过邀请码，登录入口会携带 `inviter_id`
- `账号密码登录` 会切换到邮箱 / 密码表单

账号密码登录支持：

- 输入邮箱和密码
- 密码明文 / 隐藏切换
- 登录前验证码校验
- 本地记住上次输入的邮箱和密码
- `找回密码`

普通用户登录成功后进入：

- `/console/`

### 3. 团队管理员登录

团队管理员登录表单支持：

- 输入邮箱和密码
- 密码明文 / 隐藏切换
- 登录前验证码校验
- 本地记住上次输入的邮箱和密码

团队管理员登录成功后进入：

- `/manager/`

团队管理员标签下当前没有单独的 `找回密码` 入口。

### 4. 找回密码

找回密码页路径：

- `/findpassword`

操作流程：

1. 输入邮箱
2. 完成验证码校验
3. 提交后发送重置密码邮件

### 5. 重置密码

重置密码页路径：

- `/resetpassword?token=...`

页面会先根据 `token` 读取账号信息，再要求输入：

- 新密码
- 确认密码

前端校验：

- 两次密码必须一致
- 密码长度至少 6 位

重置成功后会提示用户，并返回首页。

---

## 三、普通用户控制台

### 1. 默认入口

登录后访问 `/console` 会自动跳转到：

- `/console/tasks`

控制台布局当前为：

- 左侧侧边栏
- 顶部面包屑与操作区
- 主内容区

顶部区域包含：

- 面包屑
- 免费模型用量提示
- `获取积分`
- `刷新`

### 2. 免费模型用量提示

顶部免费模型提示会展示平台共享免费模型池的当日用量。

当前实现：

- 每天早上 10 点送出 500 亿 token 免费额度
- 所有用户共享使用
- 选择带有 `免费` 标记的模型即可使用免费额度
- 当日免费额度耗尽后，模型调用按正常模型价格扣除积分
- 前端每 30 秒请求一次 `/api/v1/users/free-model-pool/usage`

### 3. 获取积分

顶部 `获取积分` 会打开弹窗，当前列出：

- 邀请注册：`+5,000 积分 / 人`
- 征文投稿：`1 万 - 10 万积分`
- 充值积分：立即到账
- 每日签到：`+100 积分 / 天`
- 社区活动：不定期奖励

其中：

- `去邀请好友` 和 `去充值` 会打开钱包的 `我的积分`
- `加入社区交流群` 会打开技术交流群弹窗
- 征文投稿会跳转到外部活动规则文档

### 4. 左侧侧边栏

侧边栏包含：

- 顶部 MonkeyCode 标识
- 未归属任务和项目列表
- `签到领积分`
- `技术交流群`
- `配置`
- 当前用户与积分入口

任务与项目区域支持：

- 查看未归属任务
- 点击 `启动任务` 打开快速任务创建弹窗
- 展开 / 折叠 `历史任务`
- 点击 `查看更多` 进入 `/console/tasks`
- 查看项目列表
- 点击项目进入项目页
- 对项目点击 `+` 直接启动项目任务
- 点击 `添加项目` 创建项目
- 每 30 秒刷新项目、未归属任务和历史任务

任务条目支持：

- 进入任务详情
- 修改任务名称
- 对等待中 / 运行中的任务执行 `终止任务`
- 删除任务

折叠侧边栏时：

- 显示 `空项目`
- 显示 `项目列表` 或 `创建项目`

### 5. 创建项目

创建项目当前不是自由输入仓库地址，而是：

1. 输入项目名称
2. 从已绑定 Git 身份中选择身份
3. 从该身份可访问的授权仓库列表里选择仓库

如果尚未绑定 Git 身份，弹窗会提示先去 `配置` 里完成绑定。

### 6. 技术交流群

侧边栏 `技术交流群` 和顶部 `获取积分` 弹窗都可以打开社群入口。

当前展示：

- 微信群二维码
- 飞书群二维码
- 钉钉群二维码

### 7. 签到

如果当天尚未签到，侧边栏会显示 `签到领积分`。

当前实现：

- 点击后先进行验证码校验
- 签到成功获得 100 积分
- 成功后刷新钱包余额和签到状态
- 如果当天已签到，侧边栏签到入口不显示

---

## 四、钱包、套餐与积分

### 1. 钱包入口

侧边栏底部用户入口同时展示：

- 用户头像和昵称
- 当前套餐简称
- 当前可用积分总数

当前可用积分 = `长期积分` + `今日积分`。

点开后，钱包弹窗包含 5 个分区：

- `我的账户`
- `我的套餐`
- `我的积分`
- `积分记录`
- `模型定价`

### 2. 我的账户

`我的账户` 主要展示：

- 用户头像、昵称、邮箱
- 当前套餐
- 可用积分
- 所属团队或个人空间

账号操作包括：

- 修改头像
- 修改昵称
- 绑定邮箱
- 修改密码 / 设置密码
- 登出

补充说明：

- 头像必须是图片文件
- 已有密码的账号修改密码时需要输入当前密码
- 设置 / 修改用户密码时，前端要求新密码至少 8 位
- 绑定邮箱会发送绑定邮件

### 3. 我的套餐

套餐页当前展示：

- `基础版`
- `专业版`
- `旗舰版`

基础版：

- 免费
- 并发任务限制：1 个任务
- 每日积分：无

专业版：

- 价格：10,000 积分 / 月
- 并发任务限制：3 个任务
- 每日赠送 2,000 积分
- 每日积分仅当日有效，不累计

旗舰版：

- 价格：100,000 积分 / 月
- 并发任务限制：3 个任务
- 每日赠送 30,000 积分
- 每日积分仅当日有效，不累计

套餐操作：

- 非团队用户且已开通高级套餐时，顶部显示到期时间和自动续费开关
- 可直接开通专业版或旗舰版
- 开通前会弹出确认框
- 开通成功后刷新钱包、套餐状态和交易记录
- 如果当前是旗舰版，不再显示专业版开通按钮

### 4. 我的积分

`我的积分` 当前包含：

- 当前可用积分
- 邀请注册
- 社区活动
- 每日签到
- 兑换积分

当前可用积分拆分为：

- `长期积分`
- `今日积分（凌晨自动清零）`

充值：

- 点击 `充值` 打开充值弹窗
- 选择充值档位后点击 `确认充值`
- 前端向后端请求支付链接
- 请求成功后在新标签页打开支付页面

固定充值档位：

- `10,000 积分`，价格 `¥50`
- `50,000 积分`，价格 `¥200`，原价 `¥250`，页面标记为 `8 折`
- `300,000 积分`，价格 `¥1000`，原价 `¥1500`，页面标记为 `6.7 折`

邀请注册：

- 系统生成个人邀请链接：`https://monkeycode-ai.com/?ic=当前用户ID`
- 点击 `复制邀请链接` 会复制完整链接
- 好友通过该链接注册后，邀请人获得 `5000 积分`
- 支持展开查看最近邀请用户列表
- 首次请求前 20 条邀请记录

每日签到：

- 每天可签到 1 次
- 每次获得 100 积分
- 钱包内也可发起签到

兑换积分：

- 输入兑换码后点击 `兑换`
- 按回车也会触发兑换
- 兑换成功后刷新钱包余额

### 5. 积分记录

`积分记录` 用于查看积分流水。

当前实现：

- 单次请求 20 条记录
- 支持滚动加载更多
- 正向记录显示为 `+积分`
- 消耗记录显示为 `-积分`

常见记录类型包括：

- `新用户注册奖励`
- `通过兑换码领取`
- `邀请注册奖励`
- `每日签到奖励`
- `套餐升级退款`
- `当日钱包发放`
- `充值积分`
- `开发环境消耗`
- `大模型消耗`
- `MCP 工具消耗`
- `兑换专业版`
- `专业版自动续费`
- `兑换旗舰版`
- `旗舰版自动续费`

### 6. 模型定价

`模型定价` 页面用于展示不同模型与积分消耗之间的关系。

当前展示字段：

- 模型名称
- 每百万 token 消耗积分
- 代码能力分
- 标签

支持排序：

- 性价比排序
- 代码能力排序
- 按名字排序

当前内置模型价格表来自 `frontend/src/utils/common.tsx` 的 `modelPricingList`：

- `minimax-m2.7`：200 积分，标签 `免费`
- `qwen3.5-plus`：120 积分，标签 `免费`
- `gpt-5.5`：1000 积分，标签 `最新`、`很强`
- `gpt-5.4`：600 积分，标签 `能力强`
- `gpt-5.2`：400 积分
- `gpt-5.3-codex`：400 积分，标签 `能力强`、`性价比`
- `glm-5.1`：600 积分
- `glm-5`：400 积分
- `glm-4.7`：200 积分，标签 `性价比`
- `kimi-k2.6`：700 积分，标签 `新`、`能力强`
- `qwen3-max`：400 积分
- `qwen3.6-plus`：200 积分，标签 `性价比`
- `kimi-k2.5`：400 积分

说明：

- 标记为 `免费` 的模型会优先使用平台共享免费模型池
- 当共享免费额度耗尽后，会按模型正常价格扣除积分

---

## 五、智能任务

### 1. 新任务页

新任务页路径：

- `/console/tasks`

页面由两部分组成：

- 上方任务输入区
- 下方任务卡片列表

页面标题：

- `MonkeyCode 智能任务`

### 2. 创建任务

#### 任务输入

主输入框支持：

- 输入自然语言需求
- 语音输入

当前输入框没有在代码里单独处理 `Enter` 提交，主要通过点击 `执行` 创建任务。

#### 任务类型

当前有三种类型：

- `开发`
- `设计`
- `审查`

说明：

- `开发`：根据需求执行开发编码任务
- `设计`：进行架构设计，输出技术方案与设计文档
- `审查`：审查代码，识别风险，提出改进建议

#### 代码来源

`代码` 菜单当前支持三种来源：

- `ZIP 文件`
- `我的仓库`
- `其他仓库`

`ZIP 文件`：

- 仅接受 `.zip`
- 文件大小上限 10MB
- 先上传到对象存储，再作为本次任务代码来源

`我的仓库`：

- 按已绑定 Git 身份分组展示
- 支持 GitHub App、Gitee、Gitea、GitLab OAuth 身份
- 支持在每个身份下搜索仓库
- 支持刷新单个身份下的授权仓库列表
- 选中仓库后自动关联对应 Git 身份凭证
- 如果没有可选 Git 身份，会提示去设置绑定

`其他仓库`：

- 手动输入完整仓库地址
- 按回车确认
- 下方展示从任务列表中归纳出的历史仓库地址
- 如果手动输入的仓库地址匹配已绑定身份的 `base_url`，参数确认时可选择对应身份；否则可匿名

#### 技能

`技能` 按钮会打开技能选择面板。

当前实现：

- 从后端拉取技能列表
- 按标签分类展示
- 支持多选
- 部分默认技能不可取消
- 按钮上显示已选技能数量

#### 执行前参数确认

点击 `执行` 后会打开 `任务参数` 弹窗。

当前弹窗字段包括：

- 大模型
- 仓库分支
- 仓库身份凭证

仅子账号额外显示：

- 宿主机
- 系统镜像

补充说明：

- 从 `我的仓库` 里选中的仓库，不再重复要求选择身份凭证
- 使用 ZIP 代码源时，不显示仓库分支和仓库身份凭证
- 当前创建的任务固定使用 `OpenCode`
- 如果选择了平台内置模型，会强制使用平台内置宿主机
- 任务创建时固定资源为 2 核 CPU、8GB 内存、3 小时生命周期
- 上一次选择的大模型、宿主机、镜像会写入本地参数缓存，后续创建任务时复用
- 大模型字段旁边的帮助按钮可打开 `模型定价`

#### 并发限制

如果后端返回并发限制错误，会弹出并发限制提示框。

弹窗会：

- 列出当前正在运行或等待中的任务
- 允许直接终止这些任务
- 对基础版用户提供升级专业版或旗舰版入口

### 3. 快速启动任务弹窗

侧边栏 `启动任务` 会打开快速任务创建弹窗。

它和新任务页的任务输入能力基本一致，但把模型、宿主机、镜像、分支等参数收纳到 `高级选项` 区域。

弹窗关闭后会重置输入内容、代码来源、技能选择和高级选项。

### 4. 任务列表

任务列表以卡片方式展示，当前每页加载 24 条，支持无限滚动。

卡片信息包括：

- 任务名称
- 仓库名或 ZIP 文件名
- 任务状态
- Token 用量
- 创建时间

当前任务状态展示：

- `finished`：显示 `已终止`
- `error`：显示 `启动失败`
- `pending`：显示 `正在启动`
- `processing`：显示 `运行中`

卡片支持：

- 点击标题进入任务详情
- 右上角菜单修改任务名称
- 对等待中 / 运行中的任务执行 `终止`
- 对所有任务执行 `删除`
- 悬浮查看任务名称、任务内容、任务状态、任务类型、代码仓库、代码文件、仓库分支、开发工具、大模型、创建时间等详细信息

---

## 六、任务详情页

### 1. 路径

- `/console/task/:taskId`

### 2. 页面结构

当前任务详情页主要由以下区域组成：

- 顶部状态与工具栏
- 中央消息区
- 右侧文件面板
- 底部终端面板
- 在线预览弹窗

顶部按钮当前包括：

- `终端`
- `文件`
- `预览`

说明：

- `文件` 按钮会显示当前已变更文件数量
- `预览` 按钮会显示当前发现的可预览端口数量
- 当任务不处于运行中时，这三个按钮会禁用

### 3. 顶部模型与上下文

顶部左侧展示当前任务模型，并支持在任务可输入时切换模型。

切换模型流程：

1. 点击当前模型下拉菜单
2. 选择目标模型
3. 弹出确认框
4. 确认后通过任务控制连接切换模型

顶部还会展示：

- 累计消耗 token
- 上下文使用进度

当上下文使用信息可用时，点击圆形进度可打开上下文弹窗。

上下文弹窗支持：

- `压缩上下文`：向当前任务发送 `/compact`
- `重置上下文`：确认后重启会话并清空当前上下文

### 4. 环境准备状态

如果任务对应开发环境尚未就绪，主区域会显示准备视图。

当前会展示：

- 开发环境状态说明
- 最近一次环境条件消息

这部分不仅覆盖正在创建，也覆盖部分环境失败状态。

### 5. 消息区

消息区用于展示任务完整对话和工具执行信息。

支持：

- 自动附着运行中任务的实时消息流
- 加载历史轮次消息
- 通过 `加载历史消息` / `加载更多` 逐轮加载历史
- 自动滚动到底部
- 一键滚动到顶部 / 底部
- 显示计划步骤块
- 对消息错误展示悬浮错误详情
- 对失败消息提供 `尝试修复`，会重新加载会话并发送继续执行指令

如果任务已经结束、且当前没有实时流消息，页面会自动加载历史轮次。

### 6. 与 AI 对话

任务运行中且当前流未执行时，底部显示输入框。

输入区支持：

- `Enter` 发送
- `Shift + Enter` 换行
- 语音输入
- 斜杠命令插入

斜杠命令来自实时连接返回的 `availableCommands`。

执行中时：

- 文本输入框会隐藏
- 页面显示当前耗时
- 发送按钮切换为停止按钮，可发送取消命令

当任务不再处于运行中时，底部显示：

- `任务已结束`

### 7. AI 反向提问

任务消息流里支持 `ask_user_question` 类型消息。

当前实现支持：

- 单选题
- 多选题
- 自定义补充答案
- 直接在消息卡片中提交回答

如果问题来自历史消息，或实时连接已经关闭，问题会被标记为过期，无法再提交回答。

### 8. 文件面板

点击顶部 `文件` 后，会在右侧展开文件面板。

文件面板包含两个模式：

- `目录`
- `变更`

#### 目录模式

支持：

- 浏览任务工作区文件树
- 展开 / 折叠目录
- 打开文本文件只读预览
- 刷新文件树
- 在当前目录创建文件
- 创建目录
- 上传文件
- 复制文件
- 移动文件 / 目录
- 下载文件 / 目录
- 删除文件 / 目录

补充说明：

- 文件预览为只读
- 超过 100KB 的文件不支持预览
- 二进制文件不支持预览

#### 变更模式

支持：

- 查看全部已变更文件列表
- 显示新增 / 删除行数
- 点击单个文件查看 Diff
- Diff 以分栏形式展示

### 9. 终端面板

点击顶部 `终端` 后，会在页面下方展开终端区。

终端区支持：

- 查看当前任务绑定开发环境的终端会话列表
- 新建终端会话
- 切换终端会话
- 刷新会话列表
- 关闭终端会话
- 查看连接状态图标

### 10. 在线预览

点击顶部 `预览` 会打开在线预览弹窗。

弹窗支持：

- 列出已发现的监听端口
- 刷新端口列表
- 当端口已有可访问地址时直接访问
- 当端口不可访问时显示错误或暂不可访问状态

说明：

- 任务详情页里的预览主要负责展示和访问
- 端口的开放、关闭和白名单编辑在独立终端页的 `在线预览 / 端口管理` 中完成

---

## 七、Git 机器人 / 代码审查

### 1. 路径

- `/console/gitbot`

当前这个页面没有固定侧边栏入口，需要直接访问地址。

### 2. 页面页签

页面包含两个标签：

- `审查任务`
- `机器人`

### 3. 审查任务

`审查任务` 页支持：

- 查看机器人触发的审查任务列表
- 每页 24 条并支持无限滚动
- 显示任务状态
- 显示关联平台机器人
- 点击标题跳转到对应 PR / MR 页面
- 手动刷新列表

空状态下提供：

- `刷新`
- `如何使用？`

### 4. 机器人

`机器人` 页支持：

- 创建审查机器人
- 查看 Webhook 信息
- 修改机器人
- 修改查看权限
- 删除机器人

创建机器人时需要填写：

- 备注
- 宿主机
- Git 平台
- Access Token

创建成功后，页面会自动弹出 Webhook 信息，包括：

- Webhook 地址
- Secret Token

### 5. 查看权限

机器人 `权限` 弹窗支持：

- 选择团队成员
- 保存后，这些成员可以看到该机器人执行过的任务

---

## 八、项目协作

### 1. 路径

- `/console/project/:projectId`

如果项目数据异常、没有绑定仓库，当前会直接显示 `项目异常` 空状态。

### 2. 项目头部

项目头部展示：

- 项目名称
- 项目描述或 `未绑定仓库`
- 自动 Review 状态
- `启动 AI`
- 更多菜单

更多菜单包括：

- 修改名称
- 环境变量
- 开发镜像
- 移除项目

### 3. 自动 Review

点击 `未开启自动 Review / 已开启自动 Review` 会打开配置弹窗。

当前弹窗能力：

- 查看项目名称
- 切换自动 Review 开关

开启后，前端说明为：

- 自动配置仓库 Webhook
- 当提交新的 PR / MR 时自动启动 Review 任务

### 4. 启动 AI

项目页的 `启动 AI` 会打开项目专用任务弹窗。

当前可配置：

- 项目
- 代码仓库分支
- 大模型
- 任务内容

补充说明：

- 如果项目平台是 internal，不显示代码仓库分支
- 对普通 Git 平台会优先拉取分支列表
- 如果分支列表获取失败，可手动输入分支
- 提交后创建与当前项目关联的开发任务
- 项目任务生命周期为 2 小时

### 5. 项目页页签

当前项目页固定有三个页签：

- `信息`
- `需求`
- `任务`

#### 信息

`信息` 页签包含：

1. 项目文件浏览器
2. 项目根目录 `README.md` 自动加载与渲染

项目文件浏览器支持：

- 浏览项目仓库文件树
- 按分支切换查看
- 打开文件只读预览
- 跳转访问原仓库

补充说明：

- 这里是仓库浏览器，不是开发环境里的可写文件管理器
- README 会优先选择 `main`，其次 `master`，否则使用排序后的第一个分支
- 如果仓库根目录没有 `README.md`，页面会显示 `暂无文档`

#### 需求

`需求` 页签支持：

- 按状态筛选
- 按优先级筛选
- 创建需求
- 查看需求详情

状态筛选包括：

- 全部状态
- 未完成
- 已完成
- 已关闭

优先级筛选包括：

- 全部优先级
- 高
- 中
- 低

创建需求时当前字段为：

- 需求标题
- 原始需求说明（Markdown）

需求详情弹窗支持：

- 直接点击标题编辑标题
- 查看 / 编辑原始需求文档
- 查看 / 编辑技术方案文档
- 修改状态
- 修改优先级
- 查看创建者
- 查看创建时间

更多操作支持：

- 启动设计任务
- 启动开发任务
- 删除需求

设计任务会根据需求文档自动生成设计方案。

启动设计任务 / 开发任务时需要选择：

- 工作分支，内部项目除外
- AI 大模型

确认后会创建 2 小时生命周期的任务，并跳转到任务详情页。

开发任务会根据需求文档和设计文档自动生成开发任务。

#### 任务

`任务` 页签展示当前项目下的任务卡片列表。

支持：

- 查看任务详情
- 无限滚动加载
- 修改任务名称
- 删除任务

当前项目任务页签没有单独的终止任务操作。

---

## 九、配置

配置通过普通用户控制台左侧侧边栏底部 `配置` 打开。

当前配置弹窗固定有 7 个分区：

- `Git 身份`
- `MCP 与工具`
- `AI 大模型`
- `系统镜像`
- `宿主机`
- `开发环境`
- `通知`

### 1. Git 身份

当前支持的绑定方式包括：

- GitHub App 安装绑定
- Gitee OAuth 绑定
- Gitea OAuth 绑定
- GitLab OAuth 绑定
- 手动录入 GitHub / GitLab / Gitea / Gitee 身份

已绑定身份支持：

- 查看平台与地址
- 修改
- 移除

未绑定时，页面会展示对应平台的绑定卡片。

手动录入身份需要填写：

- Git 平台类型
- Git 平台地址
- Access Token
- Username
- Email
- 备注（可选）

GitHub App 安装完成后，配置弹窗会显示安装成功或失败提示，并刷新身份列表。

### 2. MCP 与工具

`MCP 与工具` 用于管理 MCP 服务器及其提供的工具能力。

当前支持：

- 查看 MCP 服务列表
- 查看内置平台 MCP 服务
- 添加用户 MCP 服务器
- 修改用户 MCP 服务器
- 同步用户 MCP 服务器
- 删除用户 MCP 服务器
- 查看服务下工具列表
- 启用 / 禁用工具
- 查看工具说明
- 查看工具调用价格，单位为 `积分/次`

添加 / 修改 MCP 服务器时需要：

- 名称
- MCP 服务器地址
- HTTP Header（JSON 对象，可选）

说明：

- 工具前缀由名称自动转拼音 / slug
- HTTP Header 必须是 JSON 对象
- 平台内置 MCP 服务不可编辑或删除，但工具开关可操作

### 3. AI 大模型

模型列表支持：

- 查看接口类型
- 查看归属类型
- 修改私有模型
- 移除私有模型
- 添加私有模型

说明：

- 公开模型会展示但不能在这里修改或移除
- 普通用户侧没有独立健康检查按钮和设为默认入口
- 添加 / 修改私有模型时需要选择接口格式、填写模型 API 地址、API Token 和模型名称
- 接口格式支持 `OpenAI Responses`、`OpenAI Chat`、`Anthropic`
- 填写 Token 后可拉取模型列表；如果接口未返回模型，也可以手动输入模型名称
- 保存添加 / 修改前会执行一次模型健康检查

### 4. 系统镜像

镜像列表支持：

- 查看镜像备注和镜像名
- 查看归属类型
- 修改私有镜像
- 移除镜像
- 添加镜像

说明：

- 公开镜像会展示但不能在这里修改
- 普通用户侧当前没有镜像设为默认入口

### 5. 宿主机

宿主机页面支持：

- 生成并复制安装命令
- 通过命令绑定 Linux 宿主机
- 查看宿主机状态和标签
- 查看宿主机上的开发环境数量
- 修改备注
- 移除私有宿主机

说明：

- 当前普通用户侧仅团队空间支持绑定宿主机
- 普通用户侧当前没有宿主机设为默认入口

### 6. 开发环境

这里指独立创建和管理的开发环境，不是任务详情页里的临时交互区。

支持：

- 创建开发环境
- 查看在线 / 离线环境
- 打开终端
- 打开文件管理
- 续期
- 移除
- 切换是否显示离线开发环境

创建开发环境时可配置：

- 宿主机
- 开发环境名称
- 仓库地址
- 仓库分支
- 仓库身份凭证
- 系统镜像
- AI 大模型
- CPU
- 内存
- 生命周期

补充说明：

- 当前仅团队空间支持创建开发环境
- 平台内置宿主机当前只允许选择不超过 3 小时的生命周期
- 平台内置宿主机最多 8 核 CPU、32GB 内存
- 其他宿主机的 CPU / 内存选项来自宿主机规格
- 生命周期可选 1 小时、2 小时、3 小时、6 小时、12 小时、1 天、3 天、7 天
- 续期弹窗当前支持追加 1 / 2 / 3 小时

### 7. 通知

用户级通知当前支持四类接收端：

- 钉钉机器人
- 飞书机器人
- 企业微信机器人
- Webhook

每个接收端支持：

- 名称
- Webhook 地址
- Secret（可选）
- 订阅事件类型

可执行操作：

- 添加
- 编辑
- 测试发送
- 移除

---

## 十、独立终端、文件管理与远程协助

### 1. 独立终端

路径：

- `/console/terminal?envid=...`

这是针对某个开发环境单独打开的终端页。

当前能力包括：

- 查看开发环境名称、状态、操作系统
- 首次进入时选择已有终端连接
- 新建终端连接
- 连接已有终端连接
- 关闭终端连接
- 手动重新连接
- 切换终端配色主题
- 打开独立文件管理
- 打开端口管理 / 在线预览
- 发起远程协助

终端连接选择弹窗中会显示：

- 终端连接 ID
- 创建时间
- 当前连接数

### 2. 端口管理

独立终端页里的 `在线预览` 实际打开端口管理弹窗。

当前支持：

- 查看端口列表
- 刷新端口列表
- 开放访问
- 访问已开放端口
- 复制访问地址
- 编辑白名单 IP
- 关闭访问 / 回收端口

补充说明：

- 开放访问时，前端会先尝试获取当前访问者 IP
- 如果获取成功，会把该 IP 写入白名单
- 白名单编辑弹窗要求每行输入一个 IP，且至少填写一个 IP

### 3. 远程协助

独立终端页支持生成远程协助连接信息。

模式有两种：

- `只读模式`：对方只能看到终端界面，无法输入或操作
- `控制模式`：对方可以看到终端界面，且可进行输入和交互

生成后会得到：

- 共享地址
- 连接密码

支持：

- 一键复制整段邀请信息

### 4. 远程协助页

路径：

- `/sharedterminal?id=...`

访问者进入后需要先输入连接密码，之后才能加入共享终端。

页面支持：

- 查看连接状态
- 查看分享者名称和头像
- 切换终端主题

### 5. 独立文件管理

路径：

- `/console/files?envid=...&path=...`

这是针对某个开发环境的完整文件管理页。

当前支持：

- 浏览目录
- 面包屑导航
- 返回上级目录
- 刷新文件列表
- 新建文件
- 新建目录
- 上传文件
- 复制文件
- 移动文件 / 目录
- 下载文件 / 目录
- 在线编辑文本文件
- 删除文件

列表展示字段包括：

- 名称
- 大小
- 用户
- 权限属性
- 修改时间

补充说明：

- 超过 1MB 的文件不允许在线编辑
- 当前独立文件管理页里，复制和删除菜单只对文件开放
- 移动和下载支持文件 / 目录

---

## 十一、IDE 辅助工具

路径：

- `/console/ide`

当前这个页面仍是占位页，但已经提供两个外链：

- 开源仓库
- 官方文档

页面文案为 `敬请期待`，不应描述为已完整交付的功能页。

---

## 十二、开发者广场

### 1. 广场列表

路径：

- `/playground`

当前已实现：

- 搜索内容
- 浏览文章 / 任务分享卡片
- 查看封面、标题、摘要
- 查看作者与浏览量
- 登录后发布内容
- 未登录点击 `发布` 会跳到登录页

### 2. 发布内容

路径：

- `/playground/create`

当前可选发布类型只有两种：

- `写一篇文章`
- `分享你执行过的任务`

说明：

- 代码里仍保留了 `project` 类型的旧状态变量，但当前 UI 没有开放项目分享入口
- 如果以 `/playground/create?taskid=...` 打开，页面会自动进入任务分享模式并预选该任务

#### 文章发布

支持：

- 标题
- Markdown 内容
- 图片上传

#### 任务分享

支持：

- 从自己的任务列表中选择任务
- 填写标题
- 填写 Markdown 内容
- 上传图片

补充说明：

- 当前前端要求至少上传 1 张图片才能发布
- 当所选任务仍在运行中，且开发环境在线时，还可以额外选择 `产出物` 文件
- 这些产出物会先被前端打包成 ZIP，再随分享一起上传
- 发布成功后提示 `审核后会显示在广场上`

## 十三、公开任务

路径：

- `/tasks/public?id=...`

这个页面当前不是独立展示页，而是一个跳转入口：

- 如果带有 `id`，会直接跳转到 `/console/task/:taskId`
- 如果没有 `id`，会跳回首页

因此不应把它描述成公开任务列表页或独立公开详情页。

---

## 十四、企业管理后台

### 1. 路径与导航

企业管理后台路径：

- `/manager`

默认进入：

- `/manager/dashboard`

左侧菜单当前包含：

- 仪表盘
- 成员管理
- 开发环境
- AI 大模型
- 系统镜像
- 管理员
- 操作记录
- 其他配置

右上角提供：

- `刷新`

左下角管理员用户菜单支持：

- 修改密码
- 登出

### 2. 仪表盘

当前仍是占位页，显示：

- `敬请期待`

### 3. 成员管理

成员管理页由两张卡片组成：

- `团队成员`
- `分组`

#### 团队成员

支持：

- 查看成员数量与上限
- 批量添加成员
- 添加时一次输入多个邮箱
- 添加成员时支持逗号、换行或分号分隔邮箱，并会校验邮箱格式
- 禁用 / 启用成员
- 为成员发送重置密码邮件
- 查看成员加入时间和最近使用时间
- 查看成员所属分组

#### 分组

支持：

- 新建分组
- 修改分组名称
- 删除分组
- 查看分组成员数量
- 查看分组成员列表
- 调整分组成员

### 4. 开发环境

团队级开发环境页面管理的是宿主机。

支持：

- 绑定宿主机
- 查看宿主机状态、规格、架构和 IP
- 查看宿主机上的开发环境数量
- 修改宿主机信息
- 删除宿主机
- 查看宿主机所属分组
- 查看宿主机上正在使用的虚拟机

修改宿主机时可配置备注和可使用该宿主机的分组。

### 5. AI 大模型

团队模型支持：

- 添加
- 修改
- 健康检查
- 删除
- 查看所属分组

添加 / 修改团队模型时可配置接口格式、模型 API 地址、API Token、模型名称和可使用该模型的分组；保存前会执行模型健康检查。

### 6. 系统镜像

团队镜像支持：

- 添加
- 修改
- 删除
- 查看所属分组

添加 / 修改团队镜像时可配置镜像名、备注和可使用该镜像的分组。

### 7. 管理员

管理员页支持：

- 查看所有管理员
- 添加管理员
- 为管理员发送密码重置邮件

### 8. 操作记录

操作记录页支持：

- 查看审计日志表格
- 查看时间、用户、操作、来源 IP
- 查看请求内容
- 查看响应内容
- 游标分页
- 切换每页条数

### 9. 其他配置

当前 `其他配置` 里已经实现的是团队级消息通知。

能力与用户侧通知基本一致，支持：

- 钉钉机器人
- 飞书机器人
- 企业微信机器人
- Webhook
- 添加
- 编辑
- 测试
- 删除
- 选择订阅事件

---
