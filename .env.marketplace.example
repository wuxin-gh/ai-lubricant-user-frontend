# 市场消费侧配置 —— 正常部署【不需要这个文件】。
#
# 前端会在运行时调 GET /api/v1/marketplace/status 拿仓库地址（该接口公开、不返回 token）。
# 只要服务端 env.ini 的 [marketplace] 配了 github_owner/github_repo/github_token，
# 前端零配置即可用，改仓库也不需要重新构建前端。
#
# 下面的变量只是【可选覆盖】，用于「前端脱离 ai-lubricant 后端单独部署」这类场景
# （拿不到 /api/v1/marketplace/status 时的兜底）。填了就以它为准，不再问服务端。
# VITE_MARKETPLACE_GITHUB_OWNER=your-org
# VITE_MARKETPLACE_GITHUB_REPO=your-marketplace-repo
# VITE_MARKETPLACE_GITHUB_BRANCH=main
# 可选，默认 modules
# VITE_MARKETPLACE_GITHUB_PATH_PREFIX=modules
